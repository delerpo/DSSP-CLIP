# DSSP-CLIP

**Zero-shot industrial anomaly detection via Dynamic Semantic Reasoning (IDSR), Cross-level Structural Prior Fusion (CSPF), and Inductive-Bias Hybrid Attention (IBHA).**

```bash
bash train.sh              # Train on VisA, test on 6 datasets
bash test.sh mvtec visa    # Zero-shot evaluation
```
